{#
/**
 * Block Name: Two Thirds Img Cta Block
 *
 * This is the template that displays the Two Thirds Img Cta block.
 */
#}

{% if is_preview %}

{% else %}

<div class="text-center mb-4">
    <h2 class="text-xl font-bold">{{ fields.title }}</h2>
</div>

<div class="flex flex-col md:flex-row sm:max-w-[1200px]">
    <div class="md:w-1/3 p-4">
        {{ fields.content }}
    </div>
    <div class="md:w-2/3 p-4">
      <div class="slick-slider">
          {% for image in fields.slider_images %}
              <div>
                  <img src="{{ image.image.url }}" alt="{{ image.image.alt }}" />
              </div>
          {% endfor %}
      </div>
    </div>
</div>


{% endif %}

<style type="text/css">

</style>

<script>
    // Initialize Slick slider with options
    jQuery(document).ready(function($) {
        $('.slick-slider').slick({
            // Add your Slick slider options here
            autoplay: true,
            dots: true,
            // Add more options as needed
        });
    });
</script>